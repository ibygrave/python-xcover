python-xcover: Exact Cover in Python
====================================

The package is built and tested by running:

    $ tox

To generate a wheel distribution install the [tox-wheel](https://pypi.org/project/tox-wheel/) plugin and run:

    $ tox --wheel

